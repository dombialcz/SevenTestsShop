{
  "name": "seven-tests-shop",
  "version": "1.0.0",
  "description": "Demo shop with React frontend and Node.js backend",
  "scripts": {
    "backend": "cd backend && npm run dev",
    "backend:seed": "cd backend && npm run seed",
    "mock-server": "node ./mock-server/mock-server.js demo-shop",
    "dev:backend": "npm run backend",
    "dev:mocks": "npm run mock-server"
  },
  "devDependencies": {
    "@ng-apimock/base-client": "^3.3.1",
    "@ng-apimock/core": "^3.12.0",
    "@ng-apimock/dev-interface": "^3.5.1",
    "express": "^5.1.0",
    "http-proxy-middleware": "^3.0.5"
  }
}
